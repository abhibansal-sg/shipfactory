#!/bin/sh
# ShipFactory self-build bootstrap: dependencies come from the operator-managed
# Hermes venv, handed to scripts as $SHIPFACTORY_PYTHON (finding #89); the
# candidate worktree needs no install step.
set -eu
exit 0
